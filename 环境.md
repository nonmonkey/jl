### 一、SDKMAN

SDKMAN 是一个强大的工具，主要用于在 macOS 和 Linux 系统上一键安装、切换和管理多个 JDK 版本，同时它也支持 Maven、Gradle 等 JVM 生态工具

```bash
# 安装 SDKMAN
curl -s "https://get.sdkman.io" | bash

# 查看版本
sdk version

# 查看可用的 Java 版本
sdk list java

# 安装 Java
sdk install java

# 切换 Java 版本（仅当前终端窗口有效）
sdk use java 17.0.14-tem

# 设置默认版本（所有新终端窗口生效）
sdk default java 21.0.4-tem

# 升级 java 到最新稳定版
sdk upgrade java

# 验证安装
java --version
javac --version
```

### 二、Maven

Maven 是 Java 开发中最核心、最通用的项目管理与构建工具。如果说 SDKMAN 是管理 Java 版本的“管家”，那 Maven 就是管理 Java 项目的“管家”。

```bash
# 清理后重新打包
mvn clean package

# 清理后安装到本地仓库
mvn clean install

# 清理后部署到远程仓库
mvn clean deploy

# 查看项目的依赖树，排查 Jar 包冲突
mvn dependency:tree
```

### 三、Gradle

### 四、Spring Boot

Spring Boot 是一个基于 Spring 框架的“脚手架”工具，它让你几乎不用配置，就能快速创建一个独立、生产级的 Spring 应用。

```bash
# 安装
sdk install springboot

# 版本
spring --version
```